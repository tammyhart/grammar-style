---
"grammar-style": minor
---

- **Utility Type `FilterToken` Exported**: Added a `FilterToken<Prefix>` generic utility mapped exclusively to mathematically constrain parameter paths downstream (e.g. `FilterToken<"color">`). This directly empowers strict scaling validation for anyone rolling their own external UI functions or composite variables seamlessly natively!
